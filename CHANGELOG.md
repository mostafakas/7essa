# سجل التغييرات

## 0.2.0 — إعادة البناء: كلمات المرور، لوحة الإدارة، GitHub + Vercel

### الدخول والحسابات
- الدخول باسم المستخدم أو رقم الموبايل مع كلمة مرور (`scrypt`) بدل رموز OTP، وحُذف مزود SMS وجدول `otp_challenges`.
- كلمات مرور مؤقتة سهلة الإملاء (`kmtr-4827`) تظهر مرة واحدة مع نسخ وطباعة، وتغيير إجباري عند أول دخول (صفحة `/account`).
- قفل 15 دقيقة بعد 5 محاولات خاطئة، ورفض الكلمات الشائعة، وإنهاء الجلسات عند تغيير كلمة المرور.
- السنتر يصدر بيانات دخول لفريقه ولأولياء الأمور والطلاب قبل أول دخول فقط؛ بعده من إدارة المنصة وحدها.
- حساب مستقل للطالب لحل الامتحانات.

### لوحة إدارة المنصة (`/platform`)
- 10 صفحات: المؤشرات، السناتر، تفاصيل السنتر (6 تبويبات)، المستخدمون، تفاصيل المستخدم (4 تبويبات)، الخطط، المدفوعات، الإعلانات، السجل، الإعدادات وصحة النظام.
- 4 أدوار لفريق الإدارة (مالك، دعم، مالية، مشاهد) و7 صلاحيات مفروضة في الخادم.
- 33 مسارًا تحت `/platform` وإجمالي 138 مسارًا موثقة في `docs/api.md`.

### الاشتراكات
- جداول جديدة: `plans`, `platform_payments`, `workspace_notes`, `announcements`, `platform_settings`.
- حالة وصول لحظية: كامل / عرض فقط / موقوف، مع فترة سماح، وحدود الخطط (طلاب وفريق) مفروضة فعليًا.
- مهمة يومية (Vercel Cron) لتذكيرات الانتهاء وتنظيف الجلسات.

### البنية والنشر
- حُذف: Supabase وسكربتاته (كانت تحوي كلمات مرور مكشوفة)، Render، Redis وBullMQ، Docker وملفات compose.
- الخادم يعمل كدالة Vercel واحدة (`apps/api/api/index.js`)، والواجهة تمرر `/api` بسر مشترك مع عنوان العميل.
- الترحيلات وضبط الدور المقيد `hessa_app` وإنشاء مالك المنصة تتم تلقائيًا مع كل نشر إنتاج.
- GitHub Actions: فحص الأنواع والاختبارات والبناء وتطبيق الترحيلات على Postgres نظيف.

### أخرى
- تصدير Excel (CSV عربي) للطلاب والمتأخرات والمساحات والمستخدمين والمدفوعات والسجل.
- شريط إعلانات ورسالة صيانة لكل المستخدمين، وتنبيهات الاشتراك داخل بوابة السنتر.
- 7 اختبارات وحدات جديدة (الإجمالي 32).

### للترقية من الإصدار السابق
1. `npm install` ثم رفع `package-lock.json` المحدث.
2. النشر يطبق الترحيل `20260918120000_accounts_platform` تلقائيًا. الحسابات القديمة بلا كلمات مرور: أصدرها من لوحة الإدارة أو من صفحة الفريق.
